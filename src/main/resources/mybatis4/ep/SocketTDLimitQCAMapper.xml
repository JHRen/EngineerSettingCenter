<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SocketTDLimitMapper">
   
	<!--表名 -->
	<sql id="tableName">
		ATC_QCA_Socket_TDLimit
	</sql>

<!-- 字段 -->
	<sql id="Field">
		ID,
		SOCKET_ID,
		CUSTOMER_CODE,
		DEVICE_NAME,
		OPERATION,
		TD_LIMIT
	</sql>

	<!-- 字段值 -->
	<sql id="FieldValue">
		#{ID},
		#{SOCKET_ID},
		#{CUSTOMER_CODE},
		#{DEVICE_NAME},
		#{OPERATION},
		#{TD_LIMIT}
	</sql>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		FROM
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and SOCKET_ID LIKE ('%' + #{pd.keywords} + '%')
		</if>
		[fhstart]order by ID desc [fhend]
	</select>

	<!-- 删除 -->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		[ID] = #{ID}
	</delete>

	<!-- 保存 -->
	<insert id="save" parameterType="pd">
		insert into
		<include refid="tableName"></include>
		  (
			SOCKET_ID,
	        CUSTOMER_CODE,
	        DEVICE_NAME,
	        OPERATION,
	        TD_LIMIT
		  )
		values
		 (
			#{SOCKET_ID},
	        #{CUSTOMER_CODE},
	        #{DEVICE_NAME},
	        #{OPERATION},
	        #{TD_LIMIT}
	     )
	</insert>

	<!-- 通过id查询 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		ID,
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where ID=#{ID}
	</select>

<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		FROM
		<include refid="tableName"></include>
		where 1=1
		<if test="keywords!= null and keywords != ''"><!-- 关键词检索 -->
			and SOCKET_ID LIKE ('%' + #{pd.keywords} + '%')
		</if>
	</select>

	<!-- 查询是否有重复主键 -->
	<select id="finBySimilar" parameterType="pd" resultType="int">
		select
		count([ID])
		from
		<include refid="tableName"></include>
		where
		1=1
		and SOCKET_ID = #{SOCKET_ID}
		and CUSTOMER_CODE = #{CUSTOMER_CODE}
		and DEVICE_NAME = #{DEVICE_NAME}
		and OPERATION = #{OPERATION}
	</select>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set
		    SOCKET_ID = #{SOCKET_ID},
            CUSTOMER_CODE = #{CUSTOMER_CODE},
            DEVICE_NAME = #{DEVICE_NAME},
            OPERATION = #{OPERATION},
            TD_LIMIT = #{TD_LIMIT},
		where
		ID=#{ID}
	</update>
</mapper>